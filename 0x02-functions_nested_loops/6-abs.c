#include "main.h"
/**
 * _abs - Computes the absolute value of an integer
 *@i: Interger to be comouted
 *Return: absolute for value of integer
 */
int _abs(int i)
{
	if (i < 0)
		i = -i;
	return (i);
}
